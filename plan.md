1. Use `replace_with_git_merge_diff` to modify `app.js` to pre-compute `_searchString` on `window.SKILLS` in `init()` and use it in `renderSkillsHub`.
2. Use `run_in_bash_session` to run basic syntax validation on `app.js` using `node -e "new Function(require('fs').readFileSync('app.js', 'utf8'))"` and execute `node build.js`.
3. Use `run_in_bash_session` to append a journal entry to `.jules/bolt.md` detailing the anti-pattern of dynamic string allocations in render loops and verify it.
4. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
5. Use the `submit` tool to create a pull request with the title `⚡ Bolt: Pre-compute search string for skills list` and description `💡 What: Pre-compute a concatenated _searchString for each skill during init() and use it in the filter loop.\n🎯 Why: Calling .toLowerCase() on multiple string properties in a render loop on every keystroke allocates many strings dynamically and blocks the main thread.\n📊 Impact: Eliminates redundant string allocations and method calls per keystroke, improving search filter performance by O(N) string operations.\n🔬 Measurement: Profile the CPU and memory allocations when typing rapidly in the skills search bar; observe fewer garbage collection pauses and lower CPU time.`
