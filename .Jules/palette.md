## 2026-08-29 - Adding ARIA labels to dynamically generated HTML elements
**Learning:** Adding ARIA labels to icon-only buttons requires checking dynamically generated HTML within JavaScript template literals (like `app.js`), as these are easily overlooked compared to static HTML files.
**Action:** Always actively search and update dynamically generated HTML within JavaScript template literals when adding ARIA labels.
