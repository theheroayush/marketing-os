## 2026-05-08 - Add ARIA Labels to Icon-Only Buttons\n**Learning:** The application extensively uses icon-only buttons (like search, notifications, sidebar items) without any accessible names, making navigation very difficult for screen reader users.\n**Action:** Add `aria-label` or `title` attributes to all icon-only buttons to ensure their purpose is clear to assistive technologies.
