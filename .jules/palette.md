## 2024-04-15 - Global Focus Visible States
**Learning:** The application lacks global keyboard focus states, making it difficult to navigate interactively via the keyboard without clicking elements. Keyboard accessibility requires clear indicators for users tabbing through elements.
**Action:** Implement `*:focus-visible` global styles in `styles.css` to add a visible outline globally to interactable elements during keyboard navigation.
